---
title: 'bundlePagesRouterDependencies'
description: 'Pages Routerのための自動依存関係バンドルを有効にする'
---

Pages Routerアプリケーションのための自動サーバーサイド依存関係バンドルを有効にします；App Routerの自動依存関係バンドルに一致します；

```js title="next.config.js"
/** @type {import('next').NextConfig} */
const nextConfig = {
  bundlePagesRouterDependencies: true,
}

module.exports = nextConfig
```

特定のパッケージをバンドルから明示的に除外するには、[`serverExternalPackages`](https://nextjs.org/docs/canary/pages/api-reference/config/next-config-js/serverExternalPackages)オプションを使用します；

## バージョン履歴 {#version-history}

| Version   | Changes                                                                                               |
| --------- | ----------------------------------------------------------------------------------------------------- |
| `v15.0.0` | 実験的機能から安定版へ移行；`bundlePagesExternals`から`bundlePagesRouterDependencies`に改名されました |
