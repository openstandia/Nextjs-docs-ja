---
title: 'loading.js'
description: 'loading.jsファイルのAPIリファレンス。'
---

**loading**ファイルは、[Suspense](/docs/app/building-your-application/routing/loading-ui-and-streaming)を基にした即時読み込み状態を作成できます。

このファイルはデフォルトで[Server Component](/docs/app/building-your-application/rendering/server-components)ですが、 `"use client"`ディレクティブを使ってClient Componentとしても利用できます。

```tsx filename="app/feed/loading.tsx" switcher
export default function Loading() {
  // またはカスタムのロード中スケルトンコンポーネント
  return <p>Loading...</p>
}
```

```jsx filename="app/feed/loading.js" switcher
export default function Loading() {
  // またはカスタムのロード中スケルトンコンポーネント
  return <p>Loading...</p>
}
```

Loading UIコンポーネントは、いかなるパラメータも受け付けません。

> **Good to know**:
>
> - ローディングUIを設計する際には、[React Developer Tools](https://react.dev/learn/react-developer-tools)を使用してSuspense boundaryを手動で切り替えると役立つかもしれません。

## バージョン履歴

| Version   | Changes        |
| --------- | -------------- |
| `v13.0.0` | `loading`が導入されました |
