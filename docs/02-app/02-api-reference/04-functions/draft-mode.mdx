---
title: 'draftMode'
description: 'draftMode関数のAPIリファレンス。'
related:
  title: '次のステップ'
  description: 'このステップバイステップガイドでDraft Modeの使い方を学びましょう。'
  links:
    - app/building-your-application/configuring/draft-mode
---

`draftMode`は、[Server Component](/docs/app/building-your-application/rendering/server-components)でDraft Modeが有効かどうかを確認することに加え、[Draft Mode](/docs/app/building-your-application/configuring/draft-mode)を有効化および無効化することができる**非同期**関数です。

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/page.ts" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/page.js" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
}
```

</TabItem>
</Tabs>

## リファレンス {#reference}

以下のメソッドとプロパティが利用可能です：

| メソッド    | 説明                                                                                       |
| ----------- | ------------------------------------------------------------------------------------------ |
| `isEnabled` | Draft Modeが有効かどうかを示すブール値。                                                   |
| `enable()`  | ルートハンドラーでcookie（`__prerender_bypass`）を設定することでDraft Modeを有効化します。 |
| `disable()` | cookieを削除することでルートハンドラーでDraft Modeを無効化します。                         |

## 知っていると良い情報 {#good-to-know}

- `draftMode`は、promiseを返す**非同期**関数です。`async/await`やReactの[`use`](https://react.dev/reference/react/use)関数を使用する必要があります。
  - バージョン14以前では、`draftMode`は同期関数でした。後方互換性のため、Next.js 15でも同期的にアクセス可能ですが、この動作は将来的に非推奨になります。
- `next build`を実行するたびに新しいバイパスcookieの値が生成されます。これにより、バイパスcookieが推測されることを防ぎます。
- HTTP上でDraft Modeをローカルでテストするには、ブラウザがサードパーティーcookieとローカルストレージへのアクセスを許可する必要があります。

## 例 {#examples}

### Draft Modeの有効化 {#enabling-draft-mode}

Draft Modeを有効化するには、新しい[Route Handler](/docs/app/building-your-application/routing/route-handlers)を作成し、`enable()`メソッドを呼び出します：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/draft/route.ts" switcher
import { draftMode } from 'next/headers'

export async function GET(request: Request) {
  const draft = await draftMode()
  draft().enable()
  return new Response('Draft mode is enabled')
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/draft/route.js" switcher
import { draftMode } from 'next/headers'

export async function GET(request) {
  const draft = await draftMode()
  draft().enable()
  return new Response('Draft mode is enabled')
}
```

</TabItem>
</Tabs>

### Draft Modeの無効化 {#disabling-draft-mode}

デフォルトでは、ブラウザを閉じるとDraft Modeのセッションは終了します。

Draft Modeを手動で無効化するには、[Route Handler](/docs/app/building-your-application/routing/route-handlers)で`disable()`メソッドを呼び出します：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/draft/route.ts" switcher
import { draftMode } from 'next/headers'

export async function GET(request: Request) {
  const draft = await draftMode()
  draft().disable()
  return new Response('Draft mode is disabled')
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/draft/route.js" switcher
import { draftMode } from 'next/headers'

export async function GET(request) {
  const draft = await draftMode()
  draft().disable()
  return new Response('Draft mode is disabled')
}
```

</TabItem>
</Tabs>

その後、リクエストを送信してルートハンドラーを呼び出します。[`<Link>` component](/docs/app/api-reference/components/link)を使ってルートを呼び出す場合、プリフェッチ時にcookieを誤って削除しないようにするために`prefetch={false}`を渡す必要があります。

### Draft Modeが有効かどうかの確認 {#checking-if-draft-mode-is-enabled}

Server Componentでは、`isEnabled`プロパティを使用してDraft Modeが有効かどうかを確認できます：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/page.ts" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/page.js" switcher
import { draftMode } from 'next/headers'

export default async function Page() {
  const { isEnabled } = await draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

</TabItem>
</Tabs>

## バージョン履歴 {#version-history}

| バージョン   | 変更点                                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| `v15.0.0-RC` | `draftMode`が現在非同期関数になりました。[codemod](/docs/app/building-your-application/upgrading/codemods#150)が利用可能です。 |
| `v13.4.0`    | `draftMode`が導入されました。                                                                                                  |
