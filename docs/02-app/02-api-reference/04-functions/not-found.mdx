---
title: notFound
description: notFound関数のAPIリファレンス
---

`notFound`関数を使用すると、ルートセグメント内で[`not-foundファイル`](/docs/app/api-reference/file-conventions/not-found)をレンダリングし、`<meta name="robots" content="noindex" />`タグを挿入することができます。

## `notFound()`

`notFound()`関数を呼び出すことで、`NEXT_NOT_FOUND`エラーをスローし、スローされたルートセグメントのレンダリングを終了します。指定された[**not-found**ファイル](/docs/app/api-reference/file-conventions/not-found)により、セグメント内でNot Found UIをレンダリングして、これらのエラーを優雅に処理することができます。

```jsx filename="app/user/[id]/page.js"
import { notFound } from 'next/navigation'

async function fetchUser(id) {
  const res = await fetch('https://...')
  if (!res.ok) return undefined
  return res.json()
}

export default async function Profile({ params }) {
  const user = await fetchUser(params.id)

  if (!user) {
    notFound()
  }

  // ...
}
```

> **Good to know**: `notFound()`はTypeScriptの[`never`](https://www.typescriptlang.org/docs/handbook/2/functions.html#never)型を使用しているため、`return notFound()`を使用する必要はありません。

## バージョン履歴

| Version   | Changes                  |
| --------- | ------------------------ |
| `v13.0.0` | `notFound`が導入されました。 |