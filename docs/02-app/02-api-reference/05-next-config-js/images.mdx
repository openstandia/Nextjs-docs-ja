---
title: images
description: next/imageローダーのカスタム設定
---

{/* このドキュメントの内容は、アプリルーターとページルーター間で共有されています。Pagesルーターに特化したコンテンツを追加するには`<PagesOnly>Content</PagesOnly>`コンポーネントを使用できます。共有コンテンツはコンポーネントでラップしないでください。 */}

Next.jsの組み込みの画像最適化APIではなく、クラウドプロバイダーを使用して画像を最適化したい場合は、次のように`next.config.js`を設定できます：

```js title="next.config.js"
module.exports = {
  images: {
    loader: 'custom',
    loaderFile: './my/image/loader.js',
  },
}
```

この`loaderFile`は、Next.jsアプリケーションのルートから相対的に指定されたファイルを指す必要があります。このファイルは文字列を返すデフォルト関数をエクスポートする必要があります。たとえば：

<AppOnly>

```js title="my/image/loader.js"
'use client'

// 画像ローダー関数
export default function myImageLoader({ src, width, quality }) {
  return `https://example.com/${src}?w=${width}&q=${quality || 75}`
}
```

また、[`loader` プロパティ](/docs/app/api-reference/components/image#loader)を使用して、`next/image`の各インスタンスに関数を渡すこともできます。

> **Good to know**: 関数を受け入れる画像ローダーファイルをカスタマイズするには、提供された関数をシリアライズするために[クライアントコンポーネント](/docs/app/building-your-application/rendering/client-components)を使用する必要があります。

組み込みの[画像最適化API](/docs/app/building-your-application/optimizing/images)と[画像コンポーネント](/docs/app/api-reference/components/image)の動作を構成する方法の詳細については、利用可能なオプションは[画像構成オプション](/docs/app/api-reference/components/image#configuration-options)を参照してください。

</AppOnly>

<PagesOnly>

```js title="my/image/loader.js"
// 画像ローダー関数
export default function myImageLoader({ src, width, quality }) {
  return `https://example.com/${src}?w=${width}&q=${quality || 75}`
}
```

また、[`loader` プロパティ](/docs/pages/api-reference/components/image#loader)を使用して、`next/image`の各インスタンスに関数を渡すこともできます。

組み込みの[画像最適化API](/docs/pages/building-your-application/optimizing/images)と[画像コンポーネント](/docs/pages/api-reference/components/image)の動作を構成する方法の詳細については、利用可能なオプションは[画像構成オプション](/docs/pages/api-reference/components/image#configuration-options)を参照してください。

</PagesOnly>

## ローダー設定の例

- [Akamai](#akamai)
- [AWS CloudFront](#aws-cloudfront)
- [Cloudinary](#cloudinary)
- [Cloudflare](#cloudflare)
- [Contentful](#contentful)
- [Fastly](#fastly)
- [Gumlet](#gumlet)
- [ImageEngine](#imageengine)
- [Imgix](#imgix)
- [PixelBin](#pixelbin)
- [Sanity](#sanity)
- [Sirv](#sirv)
- [Supabase](#supabase)
- [Thumbor](#thumbor)
- [Imagekit](#imagekitio)
- [Nitrogen AIO](#nitrogen-aio)

### Akamai

```js
// ドキュメント: https://techdocs.akamai.com/ivm/reference/test-images-on-demand
// 画像ローダー関数
export default function akamaiLoader({ src, width, quality }) {
  return `https://example.com/${src}?imwidth=${width}`
}
```

### AWS CloudFront

```js
// ドキュメント: https://aws.amazon.com/developer/application-security-performance/articles/image-optimization
// 画像ローダー関数
export default function cloudfrontLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  url.searchParams.set('format', 'auto')
  url.searchParams.set('width', width.toString())
  url.searchParams.set('quality', (quality || 75).toString())
  return url.href
}
```

### Cloudinary

```js
// デモ: https://res.cloudinary.com/demo/image/upload/w_300,c_limit,q_auto/turtles.jpg
// 画像ローダー関数
export default function cloudinaryLoader({ src, width, quality }) {
  const params = ['f_auto', 'c_limit', `w_${width}`, `q_${quality || 'auto'}`]
  return `https://example.com/${params.join(',')}${src}`
}
```

### Cloudflare

```js
// ドキュメント: https://developers.cloudflare.com/images/transform-images
// 画像ローダー関数
export default function cloudflareLoader({ src, width, quality }) {
  const params = [`width=${width}`, `quality=${quality || 75}`, 'format=auto']
  return `https://example.com/cdn-cgi/image/${params.join(',')}/${src}`
}
```

### Contentful

```js
// ドキュメント: https://www.contentful.com/developers/docs/references/images-api/
// 画像ローダー関数
export default function contentfulLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  url.searchParams.set('fm', 'webp')
  url.searchParams.set('w', width.toString())
  url.searchParams.set('q', (quality || 75).toString())
  return url.href
}
```

### Fastly

```js
// ドキュメント: https://developer.fastly.com/reference/io/
// 画像ローダー関数
export default function fastlyLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  url.searchParams.set('auto', 'webp')
  url.searchParams.set('width', width.toString())
  url.searchParams.set('quality', (quality || 75).toString())
  return url.href
}
```

### Gumlet

```js
// ドキュメント: https://docs.gumlet.com/reference/image-transform-size
// 画像ローダー関数
export default function gumletLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  url.searchParams.set('format', 'auto')
  url.searchParams.set('w', width.toString())
  url.searchParams.set('q', (quality || 75).toString())
  return url.href
}
```

### ImageEngine

```js
// ドキュメント: https://support.imageengine.io/hc/en-us/articles/360058880672-Directives
// 画像ローダー関数
export default function imageengineLoader({ src, width, quality }) {
  const compression = 100 - (quality || 50)
  const params = [`w_${width}`, `cmpr_${compression}`)]
  return `https://example.com${src}?imgeng=/${params.join('/')`
}
```

### Imgix

```js
// デモ: https://static.imgix.net/daisy.png?format=auto&fit=max&w=300
// 画像ローダー関数
export default function imgixLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  const params = url.searchParams
  params.set('auto', params.getAll('auto').join(',') || 'format')
  params.set('fit', params.get('fit') || 'max')
  params.set('w', params.get('w') || width.toString())
  params.set('q', (quality || 50).toString())
  return url.href
}
```

### PixelBin

```js
// ドキュメント (リサイズ): https://www.pixelbin.io/docs/transformations/basic/resize/#width-w
// ドキュメント (最適化): https://www.pixelbin.io/docs/optimizations/quality/#image-quality-when-delivering
// ドキュメント (自動フォーマット配信): https://www.pixelbin.io/docs/optimizations/format/#automatic-format-selection-with-f_auto-url-parameter
// 画像ローダー関数
export default function pixelBinLoader({ src, width, quality }) {
  const name = '<your-cloud-name>'
  const opt = `t.resize(w:${width})~t.compress(q:${quality || 75})`
  return `https://cdn.pixelbin.io/v2/${name}/${opt}/${src}?f_auto=true`
}
```

### Sanity

```js
// ドキュメント: https://www.sanity.io/docs/image-urls
// 画像ローダー関数
export default function sanityLoader({ src, width, quality }) {
  const prj = 'zp7mbokg'
  const dataset = 'production'
  const url = new URL(`https://cdn.sanity.io/images/${prj}/${dataset}${src}`)
  url.searchParams.set('auto', 'format')
  url.searchParams.set('fit', 'max')
  url.searchParams.set('w', width.toString())
  if (quality) {
    url.searchParams.set('q', quality.toString())
  }
  return url.href
}
```

### Sirv

```js
// ドキュメント: https://sirv.com/help/articles/dynamic-imaging/
// 画像ローダー関数
export default function sirvLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  const params = url.searchParams
  params.set('format', params.getAll('format').join(',') || 'optimal')
  params.set('w', params.get('w') || width.toString())
  params.set('q', (quality || 85).toString())
  return url.href
}
```

### Supabase

```js
// ドキュメント: https://supabase.com/docs/guides/storage/image-transformations#nextjs-loader
// 画像ローダー関数
export default function supabaseLoader({ src, width, quality }) {
  const url = new URL(`https://example.com${src}`)
  url.searchParams.set('width', width.toString())
  url.searchParams.set('quality', (quality || 75).toString())
  return url.href
}
```

### Thumbor

```js
// ドキュメント: https://thumbor.readthedocs.io/en/latest/
// 画像ローダー関数
export default function thumborLoader({ src, width, quality }) {
  const params = [`${width}x0`, `filters:quality(${quality || 75})`]
  return `https://example.com${params.join('/')}${src}`
}
```

### ImageKit.io

```js
// ドキュメント: https://imagekit.io/docs/image-transformation
// 画像ローダー関数
export default function imageKitLoader({ src, width, quality }) {
  const params = [`w-${width}`, `q-${quality || 80}`]
  return `https://ik.imagekit.io/your_imagekit_id/${src}?tr=${params.join(',')}`
}
```

### Nitrogen AIO

```js
// ドキュメント: https://docs.n7.io/aio/intergrations/
// 画像ローダー関数
export default function aioLoader({ src, width, quality }) {
  const url = new URL(src, window.location.href)
  const params = url.searchParams
  const aioParams = params.getAll('aio')
  aioParams.push(`w-${width}`)
  if (quality) {
    aioParams.push(`q-${quality.toString()}`)
  }
  params.set('aio', aioParams.join(';'))
  return url.href
}
```