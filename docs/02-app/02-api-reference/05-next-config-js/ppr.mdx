---
title: ppr
description: Next.jsでPartial Prerenderingを有効にする方法を学びましょう。
version: experimental
related:
  title: Learn more about Partial Prerendering
  links:
    - app/building-your-application/rendering/partial-prerendering
---

Partial Prerendering (PPR)は、同じルートで静的コンポーネントと動的コンポーネントを組み合わせることを可能にします。 [PPR](/docs/app/building-your-application/rendering/partial-prerendering)について詳しく学びましょう。

## Partial Prerenderingの使用

### インクリメンタルな採用 (バージョン15)

Next.js 15では、[`next.config.js`](/docs/app/API-reference/next-config-js/ppr)ファイルで[`ppr`](/docs/app/api-reference/next-config-js/ppr)オプションを`incremental`に設定し、ファイルの一番上で`experimental_ppr` [ルートコンフィグオプション](/docs/app/api-reference/file-conventions/route-segment-config)をエクスポートすることにより、Partial Prerenderingをインクリメンタルに[`レイアウト`](/docs/app/building-your-application/routing/layouts-and-templates)や[`ページ`](/docs/app/building-your-application/routing/pages)で採用できます；


<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    ppr: 'incremental',
  },
}

export default nextConfig
```
</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    ppr: 'incremental',
  },
}

module.exports = nextConfig
```
</TabItem>
</Tabs>

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/page.tsx" switcher
import { Suspense } from "react"
import { StaticComponent, DynamicComponent, Fallback } from "@/app/ui"

export const experimental_ppr = true

export default function Page() {
  return {
     <>
      <StaticComponent />
      <Suspense fallback={<Fallback />}>
        <DynamicComponent />
      </Suspense>
     </>
  };
}
```
</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/page.js" switcher
import { Suspense } from "react"
import { StaticComponent, DynamicComponent, Fallback } from "@/app/ui"

export const experimental_ppr = true

export default function Page() {
  return {
     <>
      <StaticComponent />
      <Suspense fallback={<Fallback />}>
        <DynamicComponent />
      </Suspense>
     </>
  };
}
```
</TabItem>
</Tabs>


> **Good to know**:
>
> - `experimental_ppr` がないルートはデフォルトで `false` になり、PPRを使用してプリレンダリングされません。各ルートでPPRを明示的に適用する必要があります
> - `experimental_ppr` はルートセグメントのすべての子に適用され、ネストされたレイアウトやページも含まれます。すべてのファイルに追加する必要はなく、ルートの最上位セグメントにのみ追加してください
> - 子セグメントのPPRを無効にするには、子セグメントで`experimental_ppr`を`false`に設定できます

### PPRの有効化 (バージョン14)

バージョン14では、`next.config.js`ファイルに[`ppr`](/docs/app/api-reference/next-config-js/ppr)オプションを追加することで有効化できます。PPRを有効にするにはキャナリーバージョンを使用する必要があります；これにより、アプリケーション内のすべてのルートに適用されます；

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    ppr: true,
  },
}

export default nextConfig
```
</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    ppr: true,
  },
}

module.exports = nextConfig
```
</TabItem>
</Tabs>


| バージョン   | 変更点                                      |
| --------- | ------------------------------------------- |
| `v15.0.0` | experimental `incremental`値が導入されました      |
| `v14.0.0` | experimental `ppr`が導入されました            |