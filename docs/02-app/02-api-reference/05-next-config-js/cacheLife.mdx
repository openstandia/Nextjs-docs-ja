---
title: 'cacheLife'
description: 'Next.js での cacheLife 設定方法を学びます。'
version: 'canary'
---

`cacheLife` オプションを使用すると、コンポーネントや関数内、そして [`use cache` ディレクティブの](/docs/app/api-reference/directives/use-cache)範囲内で、[`cacheLife`](/docs/app/api-reference/functions/cacheLife) 関数を使用する際に、**カスタムキャッシュプロファイル**を定義することができます。

## 使用法 {#usage}

プロファイルを定義するには、[`dynamicIO` フラグ](/docs/app/api-reference/next-config-js/dynamicIO)を有効にし、`next.config.js` ファイルの `cacheLife` オブジェクトにキャッシュプロファイルを追加します。例えば、`blog` プロファイル：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    dynamicIO: true,
    cacheLife: {
      blog: {
        stale: 3600, // 1時間
        revalidate: 900, // 15分
        expire: 86400, // 1日
      },
    },
  },
}

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
module.exports = {
  experimental: {
    dynamicIO: true,
    cacheLife: {
      blog: {
        stale: 3600, // 1時間
        revalidate: 900, // 15分
        expire: 86400, // 1日
      },
    },
  },
}
```

</TabItem>
</Tabs>

このカスタム `blog` 設定をコンポーネントや関数で次のように使用できます：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/actions.ts" highlight={4,5} switcher
import { unstable_cacheLife as cacheLife } from 'next/cache'

export async function getCachedData() {
  'use cache'
  cacheLife('blog')
  const data = await fetch('/api/data')
  return data
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/actions.js" highlight={4,5} switcher
import { unstable_cacheLife as cacheLife } from 'next/cache'

export async function getCachedData() {
  'use cache'
  cacheLife('blog')
  const data = await fetch('/api/data')
  return data
}
```

</TabItem>
</Tabs>

## リファレンス {#reference}

構成オブジェクトには以下の形式でキーの値があります：

| **プロパティ** | **値**   | **説明**                                                                                   | **要件**                                           |
| -------------- | -------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| `stale`        | `number` | サーバーをチェックせずにクライアントが値をキャッシュする期間。                             | オプション                                         |
| `revalidate`   | `number` | キャッシュがサーバーでリフレッシュすべき頻度；再検証中は古い値が提供されることがあります。 | オプション                                         |
| `expire`       | `number` | 値が動的に切り替わるまでの古い値でいられる最大期間。                                       | オプション - `revalidate` より長くなければならない |
