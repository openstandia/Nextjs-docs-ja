---
title: 'distDir'
description: 'デフォルトの.nextディレクトリの代わりに使用するカスタムビルドディレクトリを設定します。'
---

{/* このドキュメントの内容はapp routerとpages routerにまたがって共有されています。Pages Routerに特化した内容を追加するには、`<PagesOnly>Content</PagesOnly>` コンポーネントを使用できます。共有されているコンテンツはコンポーネントでラップしないでください。 */}

`.next`の代わりに使用するカスタムビルドディレクトリの名前を指定できます。

`next.config.js`を開き、`distDir`設定を追加してください：

```js title="next.config.js"
module.exports = {
  distDir: 'build', // カスタムビルドディレクトリを 'build' に設定します
}
```

これで、`next build`を実行すると、Next.jsはデフォルトの`.next`フォルダーの代わりに`build`を使用します。

> `distDir`は**プロジェクトディレクトリを離れては**なりません。たとえば、`../build`は**無効な**ディレクトリです。