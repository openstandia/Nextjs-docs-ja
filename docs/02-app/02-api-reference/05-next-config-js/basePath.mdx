---
title: 'basePath'
description: 'Next.jsアプリケーションをドメインのサブパスにデプロイするために`basePath`を使用します。'
---

{/* このドキュメントの内容はapp routerとpages routerの両方で共有されています。Pages Routerに特有の内容を追加するには、`<PagesOnly>Content</PagesOnly>`コンポーネントを使用できます。共有されているコンテンツはコンポーネントでラップしないでください。 */}

Next.jsアプリケーションをドメインのサブパスにデプロイするには、`basePath`設定オプションを使用できます。

`basePath`を使用すると、アプリケーションのパスプレフィックスを設定することができます。たとえば、`''`（空の文字列、デフォルト）の代わりに`/docs`を使用するには、`next.config.js`を開いて`basePath`設定を追加します：

```js title="next.config.js"
module.exports = {
  basePath: '/docs',
}
```

> **Good to know**: この値はビルド時に設定する必要があり、クライアントサイドのバンドルにインライン化されるため、再ビルドせずに変更することはできません。

### リンク

`next/link`や`next/router`を使用して他のページにリンクする際には、`basePath`が自動的に適用されます。

例えば、`/about`を使用すると、`basePath`が`/docs`に設定されている場合、`/docs/about`になります。

```js
export default function HomePage() {
  return (
    <>
      <Link href="/about">About Page</Link>
    </>
  )
}
```

出力されるHTML：

```html
<a href="/docs/about">About Page</a>
```

これにより、`basePath`の値を変更するときに、アプリケーション内のすべてのリンクを変更する必要がなくなります。

### 画像

<AppOnly>

[`next/image`](/docs/app/api-reference/components/image)コンポーネントを使用するとき、`src`の前に`basePath`を追加する必要があります。

</AppOnly>

<PagesOnly>

[`next/image`](https://nextjs.org/docs/canary/pages/api-reference/components/image)コンポーネントを使用するとき、`src`の前に`basePath`を追加する必要があります。

</PagesOnly>

例えば、`/docs/me.png`を使用すると、`basePath`が`/docs`に設定されている場合、画像が適切に提供されます。

```jsx
import Image from 'next/image'

function Home() {
  return (
    <>
      <h1>My Homepage</h1>
      <Image
        src="/docs/me.png"
        alt="Picture of the author"
        width={500}
        height={500}
      />
      <p>Welcome to my homepage!</p>
    </>
  )
}

export default Home
```