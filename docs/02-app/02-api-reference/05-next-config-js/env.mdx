---
title: 'env'
description: 'Next.jsアプリケーションで、ビルド時に環境変数を追加およびアクセスする方法を学びます。'
---

{/* このドキュメントの内容は、app routerとpages routerの間で共有されています。`<PagesOnly>Content</PagesOnly>`コンポーネントを使用して、Pages Routerに特化したコンテンツを追加できます。共通のコンテンツはコンポーネントでラップしないようにしてください。 */}

<AppOnly>

> [Next.js 9.4](https://nextjs.org/blog/next-9-4) のリリース以降、[環境変数を追加](/docs/app/building-your-application/configuring/environment-variables)するためのより直感的で使いやすい体験が提供されました；試してみてください！

</AppOnly>

<PagesOnly>

> [Next.js 9.4](https://nextjs.org/blog/next-9-4) のリリース以降、[環境変数を追加](https://nextjs.org/docs/canary/pages/building-your-application/configuring/environment-variables)するためのより直感的で使いやすい体験が提供されました；試してみてください！

</PagesOnly>

<AppOnly>

> **Good to know**: この方法で指定された環境変数は、**常に**JavaScriptバンドルに含まれます。環境変数名の接頭辞`NEXT_PUBLIC_`は、[環境や.envファイルを通して](/docs/app/building-your-application/configuring/environment-variables)指定する場合にのみ効果があります。

</AppOnly>

<PagesOnly>

> **Good to know**: この方法で指定された環境変数は、**常に**JavaScriptバンドルに含まれます。環境変数名の接頭辞`NEXT_PUBLIC_`は、[環境や.envファイルを通して](https://nextjs.org/docs/canary/pages/building-your-application/configuring/environment-variables)指定する場合にのみ効果があります。

</PagesOnly>

JavaScriptバンドルに環境変数を追加するには、`next.config.js`を開いて、`env`設定を追加します：

```js title="next.config.js"
module.exports = {
  env: {
    customKey: 'my-value',
  },
}
```

これで、コード内で`process.env.customKey`にアクセスできます。例えば：

```jsx
function Page() {
  return <h1>The value of customKey is: {process.env.customKey}</h1>
}

export default Page
```

Next.jsはビルド時に`process.env.customKey`を`'my-value'`に置き換えます。webpackの[DefinePlugin](https://webpack.js.org/plugins/define-plugin)の性質上、`process.env`変数を分解しようとすることは機能しません。

たとえば、次の行は：

```jsx
return <h1>The value of customKey is: {process.env.customKey}</h1>
```

最終的には以下のようになります：

```jsx
return <h1>The value of customKey is: {'my-value'}</h1>
```
