---
title: 'テスト'
description: 'Cypress、Playwright、Vitest、Jestという四つの一般的なテストツールでNext.jsをセットアップする方法を学びます。'
---

ReactとNext.jsでは、目的やユースケースに応じたさまざまなタイプのテストを書くことができます。このページでは、アプリケーションをテストするために使用できるテストの種類と、一般的に使用されるツールの概要を提供します。

## テストの種類 {#types-of-tests}

- **単体テスト** は、個々のユニット（またはコードブロック）を独立してテストすることを指します。Reactでは、ユニットは単一の関数、フック、またはコンポーネントです
  - **コンポーネントテスト** は、単体テストのより焦点を絞ったバージョンで、テストの主な対象はReactコンポーネントです。これは、コンポーネントのレンダリング方法、propsとの相互作用、ユーザーイベントに対する動作などをテストすることが含まれます
  - **統合テスト** は複数のユニットがどのように連携するかをテストすることを指します。これは、コンポーネント、フック、関数の組み合わせで行われることがあります
- **エンドツーエンド（E2E）テスト** は、ブラウザのような実際のユーザーシナリオをシミュレートする環境でユーザーフローをテストします。これは、特定のタスク（例：サインアップフロー）を本番のような環境でテストすることを意味します
- **スナップショットテスト** は、コンポーネントのレンダリングされた出力をキャプチャしてスナップショットファイルに保存します。テストが実行されるとき、コンポーネントの現在のレンダリング出力が保存されたスナップショットと比較されます。スナップショットの変更は、予期せぬ動作の変化を示すために使用されます

<AppOnly>

## 非同期サーバーコンポーネント {#async-server-components}

`async`サーバーコンポーネントはReactエコシステムにとって新しいため、一部のツールは完全には対応していません。そのため、とりあえず、`async`コンポーネントには**単体テスト**よりも**エンドツーエンドテスト**の使用を推奨します。

</AppOnly>

## ガイド {#guides}

以下のガイドを参照して、Next.jsをこれらの一般的なテストツールとセットアップする方法を学びましょう;
