---
title: 'テスト'
description: 'Next.jsをCypress、Playwright、Vitest、Jestという4つの一般的なテストツールでセットアップする方法を学びます。'
---

ReactとNext.jsでは、いくつかの異なる種類のテストがあり、それぞれに独自の目的と使用ケースがあります。このページでは、アプリケーションをテストするために使用できる種類と一般的に使用されるツールの概要を提供します。

## テストの種類 {#types-of-tests}

- **ユニットテスト**は、個々のユニット（またはコードブロック）を単独でテストすることです。Reactでは、ユニットは単一の関数、フック、またはコンポーネントであることがあります
  - **コンポーネントテスト**は、ユニットテストのより焦点を絞ったバージョンで、テストの主な対象がReactコンポーネントであるものです。これには、コンポーネントがどのようにレンダリングされるか、propsとの相互作用、ユーザーイベントに対する動作のテストが含まれることがあります
  - **統合テスト**は、複数のユニットがどのように連携するかをテストすることです。これには、コンポーネント、フック、関数の組み合わせが含まれます
- **エンドツーエンド（E2E）テスト**は、ブラウザのような実際のユーザーシナリオをシミュレートした環境でユーザーフローをテストすることです。これは、特定のタスク（例：サインアップフロー）を本番環境に近い環境でテストすることを意味します
- **スナップショットテスト**は、コンポーネントのレンダリングされた出力をキャプチャし、それをスナップショットファイルに保存することです。テストが実行されると、コンポーネントの現在のレンダリング出力を保存されたスナップショットと比較します。スナップショットの変更は、予期しない動作の変更を示すために使用されます

<AppOnly>

## 非同期Server Components {#async-server-components}

非同期Server ComponentsはReactエコシステムにおいて新しいため、一部のツールはそれらを完全にはサポートしていません。そのため、当面の間、非同期コンポーネントには**ユニットテスト**よりも**エンドツーエンドテスト**を使用することを推奨します；

</AppOnly>

## ガイド {#guides}

以下のガイドを参照して、Next.jsをこれらの一般的なテストツールでセットアップする方法を学んでください：
