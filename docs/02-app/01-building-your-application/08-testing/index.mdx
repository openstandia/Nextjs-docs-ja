---
title: 'テスト'
description: 'Next.jsをCypress、Playwright、Vitest、Jestの4つの一般的なテストツールでセットアップする方法を学びます。'
---

ReactとNext.jsでは、書くことができる異なるタイプのテストがあり、それぞれ独自の目的と使用ケースがあります。このページでは、テストのタイプとアプリケーションのテストに使用できる一般的なツールの概要を提供します。

## テストの種類 {#types-of-tests}

- **ユニットテスト**は、コードの個々のユニット（またはブロック）を個別にテストすることを含みます。Reactでは、ユニットは単一の関数、フック、またはコンポーネントであることがあります。
  - **コンポーネントテスト**は、テストの主な対象がReactコンポーネントであるユニットテストのよりフォーカスされたバージョンです。これは、コンポーネントがどのようにレンダリングされるか、propsとの相互作用、ユーザーイベントへの応答としての動作をテストすることを含む場合があります。
  - **統合テスト**は、複数のユニットがどのように一緒に機能するかをテストします。これは、コンポーネント、フック、関数の組み合わせであることがあります。
- **エンドツーエンド（E2E）テスト**は、ブラウザのような実際のユーザーシナリオをシミュレートする環境でユーザーフローをテストすることを含みます。これは、特定のタスク（例：サインアップフロー）を本番環境に近い環境でテストすることを意味します。
- **スナップショットテスト**は、コンポーネントのレンダリングされた出力をキャプチャし、スナップショットファイルに保存することを含みます。テストが実行されるとき、コンポーネントの現在のレンダリングされた出力と保存されたスナップショットが比較されます。スナップショットの変更は、予期しない動作の変化を示すために使用されます。

<AppOnly>

## 非同期Server Components {#async-server-components}

`async` Server ComponentsはReactエコシステムに新しいため、一部のツールは完全にサポートしていません。その間、`async`コンポーネントに対しては**ユニットテスト**よりも**エンドツーエンドテスト**を使用することをお勧めします。

</AppOnly>

## ガイド {#guides}

以下のガイドを参照して、Next.jsをこれらの一般的に使用されるテストツールでセットアップする方法を学びましょう：
