---
title: 'Next.js での Jest 設定'
nav_title: 'Jest'
description: 'Next.js での Jest 設定方法を学び、単体テストおよびスナップショットテストを行います。'
---

{/* このドキュメントの内容は app router と pages router で共有されています。Pages Router に特有の内容を追加する場合は、`<PagesOnly>Content</PagesOnly>` コンポーネントを使用できます。共有コンテンツはコンポーネントでラップしないでください。 */}

Jest と React Testing Library は、**単体テスト**および**スナップショットテスト**によく一緒に使われます。このガイドでは、Next.js と Jest をどのようにセットアップし、最初のテストを書くかを紹介します。

> **知っておくと良いこと**: `async` Server Components は React エコシステムでは新しいため、Jest は現時点でそれをサポートしていません。同期的な Server Component と Client Component に対して**単体テスト**を実行できますが、`async` コンポーネントには **E2E テスト**の使用を推奨します。

## クイックスタート {#quickstart}

次の例を使って、Next.js の [with-jest](https://github.com/vercel/next.js/tree/canary/examples/with-jest) サンプルで簡単に始められます：

```bash title="Terminal"
npx create-next-app@latest --example with-jest with-jest-app
```

## 手動設定 {#manual-setup}

[Next.js 12](https://nextjs.org/blog/next-12) のリリース以降、Next.js には Jest の組み込み設定が追加されました。

Jest を設定するには、`jest` と以下のパッケージを開発依存関係としてインストールします：

```bash title="Terminal"
npm install -D jest jest-environment-jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom ts-node
# または {#or}
yarn add -D jest jest-environment-jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom ts-node
# または {#or}
pnpm install -D jest jest-environment-jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom ts-node
```

以下のコマンドを実行して、基本的な Jest 設定ファイルを生成します：

```bash title="Terminal"
npm init jest@latest
# または {#or}
yarn create jest@latest
# または {#or}
pnpm create jest@latest
```

これにより、プロジェクトに対する Jest のセットアップを行う一連のプロンプトを経て、自動的に `jest.config.ts|js` ファイルが作成されます。

設定ファイルを更新して `next/jest` を使用します。このトランスフォーマーには、Next.js と共に Jest を動作させるために必要なすべての設定オプションが用意されています：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="jest.config.ts" switcher
import type { Config } from 'jest'
import nextJest from 'next/jest.js'

const createJestConfig = nextJest({
  // Next.js アプリへのパスを指定して、テスト環境で next.config.js と .env ファイルを読み込む
  dir: './',
})

// Jest に渡される任意のカスタム設定を追加
const config: Config = {
  coverageProvider: 'v8',
  testEnvironment: 'jsdom',
  // 各テストが実行される前に追加のセットアップオプションを追加
  // setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],
}

// next/jest が非同期で Next.js 設定を読み込めるように、この方法で createJestConfig をエクスポート
export default createJestConfig(config)
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="jest.config.js" switcher
const nextJest = require('next/jest')

/** @type {import('jest').Config} */
const createJestConfig = nextJest({
  // Next.js アプリへのパスを指定して、テスト環境で next.config.js と .env ファイルを読み込む
  dir: './',
})

// Jest に渡される任意のカスタム設定を追加
const config = {
  coverageProvider: 'v8',
  testEnvironment: 'jsdom',
  // 各テストが実行される前に追加のセットアップオプションを追加
  // setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],
}

// next/jest が非同期で Next.js 設定を読み込めるように、この方法で createJestConfig をエクスポート
module.exports = createJestConfig(config)
```

</TabItem>
</Tabs>

`next/jest` は、以下を含む Jest の自動設定を行います：

- [Next.js Compiler](/docs/architecture/nextjs-compiler)を使用した`transform`の設定
- スタイルシート（`.css`、`.module.css`、およびその scss バリエーション）、画像インポート、[`next/font`](/docs/app/building-your-application/optimizing/fonts)の自動モック
- `.env`（およびすべてのバリエーション）を `process.env` にロード
- テストの解決とトランスフォームから `node_modules` を無視
- テストの解決から `.next` を無視
- SWC トランスフォームが有効になるフラグのために `next.config.js` をロード

> **知っておくと良いこと**: 環境変数を直接テストするには、別のセットアップスクリプトまたは `jest.config.ts` ファイルで手動でロードしてください。詳細については、[Test Environment Variables](/docs/app/building-your-application/configuring/environment-variables#test-environment-variables)を参照してください。

<PagesOnly>

## Jest の設定（Babel を使用） {#setting-up-jest-with-babel}

[Next.js Compiler](/docs/architecture/nextjs-compiler)をオプトアウトし、代わりに Babel を使用する場合は、Jest を手動で設定し、上記のパッケージに加えて、`babel-jest` と `identity-obj-proxy` をインストールする必要があります。

Next.js 用のJestを設定するための推奨設定は以下です：

```js title="jest.config.js"
module.exports = {
  collectCoverage: true,
  // node 14.x では、カバレッジプロバイダ v8 は速度とレポートの質が良好です
  coverageProvider: 'v8',
  collectCoverageFrom: [
    '**/*.{js,jsx,ts,tsx}',
    '!**/*.d.ts',
    '!**/node_modules/**',
    '!<rootDir>/out/**',
    '!<rootDir>/.next/**',
    '!<rootDir>/*.config.js',
    '!<rootDir>/coverage/**',
  ],
  moduleNameMapper: {
    // CSS モジュールのインポートを処理
    // https://jestjs.io/docs/webpack#mocking-css-modules
    '^.+\\.module\\.(css|sass|scss)$': 'identity-obj-proxy',

    // CSS モジュールではない CSS のインポートを処理
    '^.+\\.(css|sass|scss)$': '<rootDir>/__mocks__/styleMock.js',

    // 画像のインポートを処理
    // https://jestjs.io/docs/webpack#handling-static-assets
    '^.+\\.(png|jpg|jpeg|gif|webp|avif|ico|bmp|svg)$': `<rootDir>/__mocks__/fileMock.js`,

    // モジュールエイリアスを処理
    '^@/components/(.*)$': '<rootDir>/components/$1',

    // @next/font を処理
    '@next/font/(.*)': `<rootDir>/__mocks__/nextFontMock.js`,
    // next/font を処理
    'next/font/(.*)': `<rootDir>/__mocks__/nextFontMock.js`,
    // server-only を無効化
    'server-only': `<rootDir>/__mocks__/empty.js`,
  },
  // 各テストが実行される前に追加のセットアップオプションを追加
  // setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testPathIgnorePatterns: ['<rootDir>/node_modules/', '<rootDir>/.next/'],
  testEnvironment: 'jsdom',
  transform: {
    // babel-jest を使用して対称テストを next/babel プリセットでトランスパイル
    // https://jestjs.io/docs/configuration#transform-objectstring-pathtotransformer--pathtotransformer-object
    '^.+\\.(js|jsx|ts|tsx)$': ['babel-jest', { presets: ['next/babel'] }],
  },
  transformIgnorePatterns: [
    '/node_modules/',
    '^.+\\.module\\.(css|sass|scss)$',
  ],
}
```

各設定オプションの詳細については、[Jest ドキュメント](https://jestjs.io/docs/configuration)を参照してください。また、Next.js がどのように Jest を設定するかを確認するために、[`next/jest`設定](https://github.com/vercel/next.js/blob/e02fe314dcd0ae614c65b505c6daafbdeebb920e/packages/next/src/build/jest/jest.ts)も確認することをお勧めします。

### スタイルシートと画像インポートの処理 {#handling-stylesheets-and-image-imports}

スタイルシートと画像はテストで使用されませんが、それらのインポートがエラーを引き起こす可能性があるため、モックする必要があります。

設定で参照しているモックファイル - `fileMock.js` と `styleMock.js` を `__mocks__` ディレクトリ内に作成します：

```js title="__mocks__/fileMock.js"
module.exports = 'test-file-stub'
```

```js title="__mocks__/styleMock.js"
module.exports = {}
```

静的アセットの取り扱いに関する詳細は、[Jest ドキュメント](https://jestjs.io/docs/webpack#handling-static-assets)を参照してください。

## フォントの処理 {#handling-fonts}

フォントを処理するには、`nextFontMock.js` ファイルを `__mocks__` ディレクトリ内に作成し、以下の設定を追加します：

```js title="__mocks__/nextFontMock.js"
module.exports = new Proxy(
  {},
  {
    get: function getter() {
      return () => ({
        className: 'className',
        variable: 'variable',
        style: { fontFamily: 'fontFamily' },
      })
    },
  }
)
```

</PagesOnly>

## 任意: 絶対インポートとモジュールパスエイリアスの処理 {#optional-handling-absolute-imports-and-module-path-aliases}

もしプロジェクトが[モジュールパスエイリアス](/docs/app/building-your-application/configuring/absolute-imports-and-module-aliases)を使用している場合は、`jsconfig.json` ファイルの `paths` オプションを `jest.config.js` ファイルの `moduleNameMapper` オプションと一致させる形で Jest を設定する必要があります。例：

```json title="tsconfig.json または jsconfig.json"
{
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "baseUrl": "./",
    "paths": {
      "@/components/*": ["components/*"]
    }
  }
}
```

```js title="jest.config.js"
moduleNameMapper: {
  // ...
  '^@/components/(.*)$': '<rootDir>/components/$1',
}
```

## 任意: Jest をカスタムマッチャーで拡張 {#optional-extend-jest-with-custom-matchers}

`@testing-library/jest-dom` には、`.toBeInTheDocument()` のような便利な[カスタムマッチャー](https://github.com/testing-library/jest-dom#custom-matchers)が含まれており、テストを書くのがより簡単になります。次のオプションを Jest 設定ファイルに追加することで、各テストでカスタムマッチャーをインポートできます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="jest.config.ts" switcher
setupFilesAfterEnv: ['<rootDir>/jest.setup.ts']
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="jest.config.js" switcher
setupFilesAfterEnv: ['<rootDir>/jest.setup.js']
```

</TabItem>
</Tabs>

次に、`jest.setup` 内で以下をインポートします：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="jest.setup.ts" switcher
import '@testing-library/jest-dom'
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="jest.setup.js" switcher
import '@testing-library/jest-dom'
```

</TabItem>
</Tabs>

> **知っておくと良いこと**: [`extend-expect` は `v6.0` で削除されました](https://github.com/testing-library/jest-dom/releases/tag/v6.0.0)、そのため `@testing-library/jest-dom` のバージョンが6より前を使用している場合は、代わりに `@testing-library/jest-dom/extend-expect` をインポートする必要があります。

テストの前にさらに多くのセットアップオプションを追加する必要がある場合は、それを上記の `jest.setup` ファイルに追加できます。

## `package.json` にテストスクリプトを追加 {#add-a-test-script-to-package-json}

最後に、Jest の `test` スクリプトを `package.json` ファイルに追加します：

```json title="package.json" highlight={6-7}
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "jest",
    "test:watch": "jest --watch"
  }
}
```

`jest --watch` はファイルが変更されたときにテストを再実行します。その他の Jest CLI オプションについては、[Jest Docs](https://jestjs.io/docs/cli#reference) を参照してください。

### 初めてのテストの作成 {#creating-your-first-test}

これでプロジェクトはテストを実行する準備ができました。プロジェクトの root ディレクトリに `__tests__` フォルダを作成します。

<PagesOnly>

例えば、`<Home />` コンポーネントが見出しを正しくレンダリングしているかを確認するテストを追加できます：

```jsx title="pages/index.js
export default function Home() {
  return <h1>Home</h1>
}
```

```jsx filename="__tests__/index.test.js"
import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'
import Home from '../pages/index'

describe('Home', () => {
  it('見出しをレンダリングする', () => {
    render(<Home />)

    const heading = screen.getByRole('heading', { level: 1 })

    expect(heading).toBeInTheDocument()
  })
})
```

</PagesOnly>

<AppOnly>

例えば、`<Page />` コンポーネントが見出しを正しくレンダリングしているかを確認するテストを追加できます：

```jsx title="app/page.js"
import Link from 'next/link'

export default function Page() {
  return (
    <div>
      <h1>Home</h1>
      <Link href="/about">About</Link>
    </div>
  )
}
```

```jsx title="__tests__/page.test.jsx"
import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'
import Page from '../app/page'

describe('Page', () => {
  it('見出しをレンダリングする', () => {
    render(<Page />)

    const heading = screen.getByRole('heading', { level: 1 })

    expect(heading).toBeInTheDocument()
  })
})
```

</AppOnly>

オプションとして、コンポーネント内の予期しない変更を追跡するための[スナップショットテスト](https://jestjs.io/docs/snapshot-testing)を追加します：

<PagesOnly>

```jsx title="__tests__/snapshot.js"
import { render } from '@testing-library/react'
import Home from '../pages/index'

it('ホームページが変更されずにレンダリングされる', () => {
  const { container } = render(<Home />)
  expect(container).toMatchSnapshot()
})
```

> **知っておくと良いこと**: Pages Router 内にテストファイルを含めるべきではありません。Pages Router 内のファイルはすべてルートとみなされるからです。

</PagesOnly>

<AppOnly>

```jsx title="__tests__/snapshot.js"
import { render } from '@testing-library/react'
import Page from '../app/page'

it('ホームページが変更されずにレンダリングされる', () => {
  const { container } = render(<Page />)
  expect(container).toMatchSnapshot()
})
```

</AppOnly>

## テストの実行 {#running-your-tests}

次に、以下のコマンドを実行してテストを実行します：

```bash title="Terminal"
npm run test
# または {#or}
yarn test
# または {#or}
pnpm test
```

## 追加リソース {#additional-resources}

さらに詳しい情報をお探しの場合は、以下のリソースが役立ちます：

- [Next.js with Jest の例](https://github.com/vercel/next.js/tree/canary/examples/with-jest)
- [Jest ドキュメンテーション](https://jestjs.io/docs/getting-started)
- [React Testing Library ドキュメンテーション](https://testing-library.com/docs/react-testing-library/intro/)
- [Testing Playground](https://testing-playground.com/) - 良いテストの実践で要素をマッチングします。
