---
title: 'public フォルダ'
nav_title: 'public'
description: 'Next.jsでは、publicディレクトリ内で画像などの静的ファイルを提供できます。ここでその仕組みを学べます。'
---

{/* このドキュメントの内容は、app routerとpages routerの間で共有されています。Pages Routerに特化した内容を追加するには、`<PagesOnly>Content</PagesOnly>`コンポーネントを使用できます。共有されるコンテンツはコンポーネントでラップしないでください。 */}

Next.jsでは、`public`というフォルダ内で画像などの静的ファイルを提供できます。`public`内のファイルは、ベースURL（`/`）から始まるパスでコードから参照できます。

たとえば、`public/avatars/me.png`というファイルは、`/avatars/me.png`というパスで表示できます。その画像を表示するコードは次のようになります：

```jsx title="avatar.js"
import Image from 'next/image'

export function Avatar({ id, alt }) {
  return <Image src={`/avatars/${id}.png`} alt={alt} width="64" height="64" />
}

export function AvatarOfMe() {
  return <Avatar id="me" alt="A portrait of me" />
}
```

## キャッシング {#caching}

Next.jsは、`public`フォルダ内のアセットを安全にキャッシュすることができません。なぜなら、それらは変更される可能性があるからです。適用されるデフォルトのキャッシュヘッダーは次のとおりです：

```jsx
Cache-Control: public, max-age=0
```

## Robots、Favicons、その他 {#robots-favicons-and-others}

<PagesOnly>

このフォルダは、`robots.txt`、`favicon.ico`、Google Site Verification、その他の静的ファイル（`.html`を含む）にも便利です。ただし、`pages/`ディレクトリ内のファイルと同じ名前の静的ファイルを持たないようにしてください。これはエラーの原因となります。[詳細はこちら](https://nextjs.org/docs/messages/conflicting-public-file-page)。

</PagesOnly>

<AppOnly>

`robots.txt`、`favicon.ico`などの静的メタデータファイルについては、`app`フォルダ内の[特別なメタデータファイル](/docs/app/api-reference/file-conventions/metadata)を使用する必要があります。

</AppOnly>
