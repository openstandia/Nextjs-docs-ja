---
title: 'cssChunking'
description: 'Next.jsアプリケーションでCSSファイルのチャンクを制御するために`cssChunking`オプションを使用します。'
version: 'experimental'
---

CSS Chunkingは、CSSファイルをチャンクに分割して再配置することで、Webアプリケーションのパフォーマンスを向上させる手法です。これにより、アプリケーション全体のCSSを一度に読み込むのではなく、特定のルートに必要なCSSのみを読み込むことができます。

`next.config.js`ファイルで、`experimental.cssChunking`オプションを使用して、CSSファイルのチャンク方法を制御できます：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig = {
  experimental: {
    cssChunking: true, // デフォルト
  },
} satisfies NextConfig

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    cssChunking: true, // デフォルト
  },
}

module.exports = nextConfig
```

</TabItem>
</Tabs>

## オプション {#options}

- **`true`（デフォルト）**: Next.jsは可能な限りCSSファイルをマージしようとし、ファイル間の明示的および暗黙的な依存関係をインポート順から判断して、チャンク数とリクエスト数を減らします
- **`false`**: Next.jsはCSSファイルをマージしたり順序を変更したりしません
- **`'strict'`**: Next.jsはインポートされたファイルの順序でCSSファイルを正しく読み込みます。このため、チャンクおよびリクエストが増える可能性があります

予期しないCSSの動作に遭遇した場合、`'strict'`の使用を検討することがあります。例えば、異なるファイルで異なる`import`順序（`a`が`b`の前、またはその逆）で`a.css`と`b.css`をインポートする場合、`true`はどの順序でもファイルをマージし、それらの間に依存関係がないと仮定します。しかし、`b.css`が`a.css`に依存している場合、ファイルをマージせずにインポート順にロードするために、`'strict'`を使用することが望ましいかもしれません—これにより、チャンクおよびリクエストが増える可能性があります。

ほとんどのアプリケーションでは、`true`を推奨します。これにより、リクエスト数が減り、パフォーマンスが向上します。
