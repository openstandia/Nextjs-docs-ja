---
title: 'staticGeneration*'
description: 'Next.jsアプリケーションで静的生成を設定する方法を学びましょう。'
version: 'experimental'
---

`staticGeneration*`オプションを使用すると、高度なユースケースに対して静的生成プロセスを設定できます。

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

// 静的生成の設定を行います
const nextConfig: NextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
// 静的生成の設定を行います
const nextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

</TabItem>
</Tabs>

## 設定オプション {#config-options}

次のオプションを使用できます：

- `staticGenerationRetryCount`: ページ生成が失敗したときにビルドを失敗させる前に再試行する回数
- `staticGenerationMaxConcurrency`: ワーカーごとに処理する最大ページ数
- `staticGenerationMinPagesPerWorker`: 新しいワーカーを開始する前に処理するページの最小数
