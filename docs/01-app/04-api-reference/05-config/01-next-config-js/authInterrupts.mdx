---
title: 'authInterrupts'
description: '実験的な `authInterrupts` 構成オプションを有効にして、`forbidden` および `unauthorized` を使用する方法を学びます'
version: 'canary'
related:
  links:
    - app/api-reference/functions/forbidden
    - app/api-reference/functions/unauthorized
    - app/api-reference/file-conventions/forbidden
    - app/api-reference/file-conventions/unauthorized
---

`authInterrupts` 構成オプションを使用すると、アプリケーションで [`forbidden`](/docs/app/api-reference/functions/forbidden) および [`unauthorized`](/docs/app/api-reference/functions/unauthorized) API を使用できます。これらの関数は実験的なものなので、使用するには `next.config.js` ファイルで `authInterrupts` オプションを有効にする必要があります：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

// nextConfigを設定する
const nextConfig: NextConfig = {
  experimental: {
    authInterrupts: true, // authInterruptsを有効にする
  },
}

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
module.exports = {
  experimental: {
    authInterrupts: true, // authInterruptsを有効にする
  },
}
```

</TabItem>
</Tabs>
