---
title: 'generateSitemaps'
nav_title: 'generateSitemaps'
description: 'generateSitemaps 関数を使用して、アプリケーション用に複数のサイトマップを作成する方法を学ぶ'
related:
  title: 'Next Steps'
  description: 'Next.js アプリケーション用のサイトマップを作成する方法を学ぶ'
  links:
    - app/api-reference/file-conventions/metadata/sitemap
---

`generateSitemaps` 関数を使用して、アプリケーション用に複数のサイトマップを生成することができます。

## 戻り値 {#returns}

`generateSitemaps` は `id` プロパティを持つオブジェクトの配列を返します。

## URLs {#urls}

生成されたサイトマップは `/.../sitemap/[id].xml` で利用可能です。たとえば、`/product/sitemap/1.xml` のようになります。

## 例 {#example}

たとえば、`generateSitemaps` を使用してサイトマップを分割し、サイトマップの `id` を持つオブジェクトの配列を返します。次に、`id` を使用してユニークなサイトマップを生成します。

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="app/product/sitemap.ts" switcher
import { BASE_URL } from '@/app/lib/constants'

export async function generateSitemaps() {
  // 総製品数を取得し、必要なサイトマップの数を計算します
  return [{ id: 0 }, { id: 1 }, { id: 2 }, { id: 3 }]
}

export default async function sitemap({
  id,
}: {
  id: number
}): Promise<MetadataRoute.Sitemap> {
  // Google の制限は、サイトマップごとに 50,000 URLs です
  const start = id * 50000
  const end = start + 50000
  const products = await getProducts(
    `SELECT id, date FROM products WHERE id BETWEEN ${start} AND ${end}`
  )
  return products.map((product) => ({
    url: `${BASE_URL}/product/${product.id}`,
    lastModified: product.date,
  }))
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/product/sitemap.js" switcher
import { BASE_URL } from '@/app/lib/constants'

export async function generateSitemaps() {
  // 総製品数を取得し、必要なサイトマップの数を計算します
  return [{ id: 0 }, { id: 1 }, { id: 2 }, { id: 3 }]
}

export default async function sitemap({ id }) {
  // Google の制限は、サイトマップごとに 50,000 URLs です
  const start = id * 50000
  const end = start + 50000
  const products = await getProducts(
    `SELECT id, date FROM products WHERE id BETWEEN ${start} AND ${end}`
  )
  return products.map((product) => ({
    url: `${BASE_URL}/product/${id}`,
    lastModified: product.date,
  }))
}
```

</TabItem>
</Tabs>

## バージョン履歴 {#version-history}

| Version   | Changes                                                                                                                                                                   |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `v15.0.0` | `generateSitemaps` が開発環境と本番環境で一貫性のあるURLsを生成するようになりました                                                                                       |
| `v13.3.2` | `generateSitemaps` が導入されました。開発環境では、生成されたサイトマップを `/.../sitemap.xml/[id]` で表示できます。たとえば、`/product/sitemap.xml/1` のようになります。 |
