---
title: 'unauthorized.js'
description: 'unauthorized.js 特殊ファイルのAPIリファレンス。'
related:
  links:
    - app/api-reference/functions/unauthorized
version: 'canary'
---

**unauthorized** ファイルは、認証中に [`unauthorized`](/docs/app/api-reference/functions/unauthorized) 関数が呼び出されたときにUIをレンダリングするために使用されます。Next.jsでは、UIをカスタマイズできるだけでなく、`401`ステータスコードも返します。

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/unauthorized.tsx" switcher
import Login from '@/app/components/Login'

export default function Unauthorized() {
  return (
    <main>
      <h1>401 - Unauthorized</h1>
      <p>このページにアクセスするにはログインしてください。</p>
      <Login />
    </main>
  )
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/unauthorized.js" switcher
import Login from '@/app/components/Login'

export default function Unauthorized() {
  return (
    <main>
      <h1>401 - Unauthorized</h1>
      <p>このページにアクセスするにはログインしてください。</p>
      <Login />
    </main>
  )
}
```

</TabItem>
</Tabs>

## リファレンス {#reference}

### Props {#props}

`unauthorized.js` コンポーネントは、propsを受け付けません。

## 例 {#examples}

### 未認証ユーザーへのログインUIの表示 {#displaying-login-ui-to-unauthenticated-users}

[`unauthorized`](/docs/app/api-reference/functions/unauthorized) 関数を使用して、ログインUIを含む `unauthorized.js` ファイルをレンダリングできます。

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/dashboard/page.tsx" switcher
import { verifySession } from '@/app/lib/dal'
import { unauthorized } from 'next/server'

export default async function DashboardPage() {
  const session = await verifySession()

  if (!session) {
    unauthorized()
  }

  return <div>Dashboard</div>
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/dashboard/page.js" switcher
import { verifySession } from '@/app/lib/dal'
import { unauthorized } from 'next/server'

export default async function DashboardPage() {
  const session = await verifySession()

  if (!session) {
    unauthorized()
  }

  return <div>Dashboard</div>
}
```

</TabItem>
</Tabs>

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/unauthorized.tsx" switcher
import Login from '@/app/components/Login'

export default function UnauthorizedPage() {
  return (
    <main>
      <h1>401 - Unauthorized</h1>
      <p>このページにアクセスするにはログインしてください。</p>
      <Login />
    </main>
  )
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/unauthorized.js" switcher
import Login from '@/app/components/Login'

export default function UnauthorizedPage() {
  return (
    <main>
      <h1>401 - Unauthorized</h1>
      <p>このページにアクセスするにはログインしてください。</p>
      <Login />
    </main>
  )
}
```

</TabItem>
</Tabs>

## バージョン履歴 {#version-history}

| バージョン | 変更点                             |
| ---------- | ---------------------------------- |
| `v15.1.0`  | `unauthorized.js` が導入されました |
