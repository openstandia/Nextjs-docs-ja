---
title: 'not-found.js'
description: 'not-found.jsファイルのAPIリファレンス'
---

**not-found** ファイルは、[`notFound`](/docs/app/api-reference/functions/not-found) 関数がルートセグメント内で投げられたときにUIをレンダリングするために使用されます。カスタムUIを提供するだけでなく、Next.jsはストリームレスポンスには`200`HTTPステータスコードを、非ストリームレスポンスには`404`を返します。

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/not-found.tsx" switcher
import Link from 'next/link'

// リソースが見つからなかった場合に表示されるカスタムページ
export default function NotFound() {
  return (
    <div>
      <h2>Not Found</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <Link href="/">ホームに戻る</Link>
    </div>
  )
}
```

</TabItem>
</Tabs>
<Tabs>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/blog/not-found.js" switcher
import Link from 'next/link'

// リソースが見つからなかった場合に表示されるカスタムページ
export default function NotFound() {
  return (
    <div>
      <h2>Not Found</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <Link href="/">ホームに戻る</Link>
    </div>
  )
}
```

</TabItem>
</Tabs>

## リファレンス {#reference}

### Props {#props}

`not-found.js` コンポーネントは、いかなるpropsも受け付けません。

> **Good to know**: 期待される`notFound()`エラーをキャッチすることに加えて、root `app/not-found.js`ファイルはアプリケーション全体の一致しないURLも処理します。これは、アプリによって処理されないURLにアクセスしたユーザーには、`app/not-found.js`ファイルによってエクスポートされたUIが表示されることを意味します。

## 例 {#examples}

### データフェッチング {#data-fetching}

デフォルトでは、`not-found` は Server Componentです。データを取得して表示するために `async` としてマークできます：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/not-found.tsx" switcher
import Link from 'next/link'
import { headers } from 'next/headers'

// サーバーからデータをフェッチして表示する例
export default async function NotFound() {
  const headersList = await headers()
  const domain = headersList.get('host')
  const data = await getSiteData(domain)
  return (
    <div>
      <h2>Not Found: {data.name}</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <p>
        <Link href="/blog">すべての投稿を見る</Link>
      </p>
    </div>
  )
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/not-found.jsx" switcher
import Link from 'next/link'
import { headers } from 'next/headers'

// サーバーからデータをフェッチして表示する例
export default async function NotFound() {
  const headersList = await headers()
  const domain = headersList.get('host')
  const data = await getSiteData(domain)
  return (
    <div>
      <h2>Not Found: {data.name}</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <p>
        <Link href="/blog">すべての投稿を見る</Link>
      </p>
    </div>
  )
}
```

</TabItem>
</Tabs>

パスに基づいて内容を表示するために、`usePathname`のようなClient Componentフックを使用する必要がある場合は、クライアントサイドでデータをフェッチする必要があります。

## バージョン履歴 {#version-history}

| バージョン | 変更点                                                         |
| ---------- | -------------------------------------------------------------- |
| `v13.3.0`  | Root `app/not-found` がグローバルな一致しないURLを処理します。 |
| `v13.0.0`  | `not-found`が導入されました                                    |
