---
title: 'not-found.js'
description: 'not-found.jsファイルのAPIリファレンス。'
---

**not-found**ファイルは、ルートセグメント内で[`notFound`](/docs/app/api-reference/functions/not-found)関数がスローされたときにUIをレンダリングするために使用されます。カスタムUIを提供するだけでなく、Next.jsはストリームレスポンスには`200`のHTTPステータスコードを返し、非ストリームレスポンスには`404`を返します。

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/not-found.tsx" switcher
import Link from 'next/link'

// リクエストされたリソースが見つからない場合に表示されるUIを提供します
export default function NotFound() {
  return (
    <div>
      <h2>Not Found</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <Link href="/">ホームに戻る</Link>
    </div>
  )
}
```

</TabItem>
</Tabs>
<Tabs>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/blog/not-found.js" switcher
import Link from 'next/link'

// リクエストされたリソースが見つからない場合に表示されるUIを提供します
export default function NotFound() {
  return (
    <div>
      <h2>Not Found</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <Link href="/">ホームに戻る</Link>
    </div>
  )
}
```

</TabItem>
</Tabs>

## リファレンス {#reference}

### Props {#props}

`not-found.js`コンポーネントは、どのpropsも受け付けません。

> **Good to know**: 期待される`notFound()`エラーをキャッチするだけでなく、rootの`app/not-found.js`ファイルはアプリケーション全体で一致しないURLも処理します。つまり、アプリケーションで処理されていないURLにアクセスしたユーザーには、`app/not-found.js`ファイルでエクスポートされるUIが表示されます。

## 例 {#examples}

### データフェッチング {#data-fetching}

デフォルトでは、`not-found`はServer Componentです。データをフェッチして表示するために`async`としてマークすることができます：

<Tabs>
<TabItem value="tsx" label="TypeScript">

```tsx title="app/not-found.tsx" switcher
import Link from 'next/link'
import { headers } from 'next/headers'

// リクエストされたリソースが見つからない場合に表示されるUIを提供し、データをフェッチします
export default async function NotFound() {
  const headersList = await headers()
  const domain = headersList.get('host')
  const data = await getSiteData(domain)
  return (
    <div>
      <h2>Not Found: {data.name}</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <p>
        <Link href="/blog">すべての投稿を見る</Link>
      </p>
    </div>
  )
}
```

</TabItem>
<TabItem value="jsx" label="JavaScript">

```jsx title="app/not-found.jsx" switcher
import Link from 'next/link'
import { headers } from 'next/headers'

// リクエストされたリソースが見つからない場合に表示されるUIを提供し、データをフェッチします
export default async function NotFound() {
  const headersList = await headers()
  const domain = headersList.get('host')
  const data = await getSiteData(domain)
  return (
    <div>
      <h2>Not Found: {data.name}</h2>
      <p>リクエストされたリソースが見つかりませんでした</p>
      <p>
        <Link href="/blog">すべての投稿を見る</Link>
      </p>
    </div>
  )
}
```

</TabItem>
</Tabs>

パスに基づいてコンテンツを表示するために`usePathname`のようなClient Componentフックを使用する場合は、クライアントサイドでデータをフェッチする必要があります。

## バージョン履歴 {#version-history}

| バージョン | 変更点                                                      |
| ---------- | ----------------------------------------------------------- |
| `v13.3.0`  | Root `app/not-found`はグローバルに一致しないURLを処理します |
| `v13.0.0`  | `not-found`が導入されました                                 |
