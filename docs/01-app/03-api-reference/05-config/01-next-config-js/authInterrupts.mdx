---
title: 'authInterrupts'
description: '実験的な `authInterrupts` 設定オプションを有効にして、`forbidden` と `unauthorized` を使用する方法を学びます'
version: 'canary'
related:
  links:
    - app/api-reference/functions/forbidden
    - app/api-reference/functions/unauthorized
    - app/api-reference/file-conventions/forbidden
    - app/api-reference/file-conventions/unauthorized
---

`authInterrupts` 設定オプションは、アプリケーションで [`forbidden`](/docs/app/api-reference/functions/forbidden) と [`unauthorized`](/docs/app/api-reference/functions/unauthorized) API を使用することを可能にします。これらの関数は実験的なため、使用するには `next.config.js` ファイルで `authInterrupts` オプションを有効にする必要があります：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

// 実験的な 'authInterrupts' を有効にしています
const nextConfig: NextConfig = {
  experimental: {
    authInterrupts: true,
  },
}

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
// 実験的な 'authInterrupts' を有効にしています
module.exports = {
  experimental: {
    authInterrupts: true,
  },
}
```

</TabItem>
</Tabs>
