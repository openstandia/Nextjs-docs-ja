---
title: 'ESLintプラグイン'
nav_title: 'ESLint'
description: 'Next.jsアプリケーションで一般的な問題やトラブルをキャッチするためのESLintプラグインの使用方法と設定方法を学びます。'
---

{/* このドキュメントの内容は、app と pages router の両方で共有されています。Pages Routerに特定の内容を追加するには、`<PagesOnly>Content</PagesOnly>` コンポーネントを使用できます。共通のコンテンツはコンポーネントでラップしないでください。 */}

Next.jsは、Next.jsアプリケーションに共通する問題やトラブルをキャッチできるESLintプラグイン、 [`eslint-plugin-next`](https://www.npmjs.com/package/@next/eslint-plugin-next)をベースの設定にすでにバンドルして提供しています。

## 参照 {#reference}

次のESLintプラグインからの推奨ルールセットはすべて、`eslint-config-next`内で使用されます：

- [`eslint-plugin-react`](https://www.npmjs.com/package/eslint-plugin-react)
- [`eslint-plugin-react-hooks`](https://www.npmjs.com/package/eslint-plugin-react-hooks)
- [`eslint-plugin-next`](https://www.npmjs.com/package/@next/eslint-plugin-next)

これらは`next.config.js`の設定よりも優先されます。

### ルール {#rules}

ルールの完全なセットは次のとおりです：

| 推奨設定で有効化されている | ルール                                                                                                                                     | 説明                                                                                                                                  |
| :------------------------: | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
|    <Check size={18} />     | [@next/next/google-font-display](https://nextjs.org/docs/messages/google-font-display)                                                     | Googleフォントでfont-displayの動作を強制します。                                                                                      |
|    <Check size={18} />     | [@next/next/google-font-preconnect](https://nextjs.org/docs/messages/google-font-preconnect)                                               | Googleフォントで`preconnect`が使用されていることを確認します。                                                                        |
|    <Check size={18} />     | [@next/next/inline-script-id](https://nextjs.org/docs/messages/inline-script-id)                                                           | インラインコンテンツを持つ`next/script`コンポーネントで`id`属性を強制します。                                                         |
|    <Check size={18} />     | [@next/next/next-script-for-ga](https://nextjs.org/docs/messages/next-script-for-ga)                                                       | Google Analytics用のインラインスクリプトを使用する際に`next/script`コンポーネントを推奨します。                                       |
|    <Check size={18} />     | [@next/next/no-assign-module-variable](https://nextjs.org/docs/messages/no-assign-module-variable)                                         | `module`変数への代入を防ぎます。                                                                                                      |
|    <Check size={18} />     | [@next/next/no-async-client-component](https://nextjs.org/docs/messages/no-async-client-component)                                         | client componentを非同期関数にすることを防ぎます。                                                                                    |
|    <Check size={18} />     | [@next/next/no-before-interactive-script-outside-document](https://nextjs.org/docs/messages/no-before-interactive-script-outside-document) | `pages/_document.js`以外での`next/script`の`beforeInteractive`戦略の使用を防ぎます。                                                  |
|    <Check size={18} />     | [@next/next/no-css-tags](https://nextjs.org/docs/messages/no-css-tags)                                                                     | 手動のスタイルシートタグを防ぎます。                                                                                                  |
|    <Check size={18} />     | [@next/next/no-document-import-in-page](https://nextjs.org/docs/messages/no-document-import-in-page)                                       | `pages/_document.js`以外での`next/document`のインポートを防ぎます。                                                                   |
|    <Check size={18} />     | [@next/next/no-duplicate-head](https://nextjs.org/docs/messages/no-duplicate-head)                                                         | `pages/_document.js`における`<Head>`の重複使用を防ぎます。                                                                            |
|    <Check size={18} />     | [@next/next/no-head-element](https://nextjs.org/docs/messages/no-head-element)                                                             | `<head>`要素の使用を防ぎます。                                                                                                        |
|    <Check size={18} />     | [@next/next/no-head-import-in-document](https://nextjs.org/docs/messages/no-head-import-in-document)                                       | `pages/_document.js`での`next/head`の使用を防ぎます。                                                                                 |
|    <Check size={18} />     | [@next/next/no-html-link-for-pages](https://nextjs.org/docs/messages/no-html-link-for-pages)                                               | 内部のNext.jsページへの移動で`<a>`要素を使用することを防ぎます。                                                                      |
|    <Check size={18} />     | [@next/next/no-img-element](https://nextjs.org/docs/messages/no-img-element)                                                               | `<img>`要素の使用を、LCPが遅くなり、帯域幅が増えるため防ぎます。                                                                      |
|    <Check size={18} />     | [@next/next/no-page-custom-font](https://nextjs.org/docs/messages/no-page-custom-font)                                                     | ページ専用カスタムフォントを防ぎます。                                                                                                |
|    <Check size={18} />     | [@next/next/no-script-component-in-head](https://nextjs.org/docs/messages/no-script-component-in-head)                                     | `next/head`コンポーネントでの`next/script`の使用を防ぎます。                                                                          |
|    <Check size={18} />     | [@next/next/no-styled-jsx-in-document](https://nextjs.org/docs/messages/no-styled-jsx-in-document)                                         | `pages/_document.js`での`styled-jsx`の使用を防ぎます。                                                                                |
|    <Check size={18} />     | [@next/next/no-sync-scripts](https://nextjs.org/docs/messages/no-sync-scripts)                                                             | 同期スクリプトを防ぎます。                                                                                                            |
|    <Check size={18} />     | [@next/next/no-title-in-document-head](https://nextjs.org/docs/messages/no-title-in-document-head)                                         | `next/document`の`Head`コンポーネントでの`<title>`使用を防ぎます。                                                                    |
|    <Check size={18} />     | @next/next/no-typos                                                                                                                        | [Next.jsのデータ取得関数](https://nextjs.org/docs/canary/pages/building-your-application/data-fetching)での一般的なタイポを防ぎます。 |
|    <Check size={18} />     | [@next/next/no-unwanted-polyfillio](https://nextjs.org/docs/messages/no-unwanted-polyfillio)                                               | Polyfill.ioからの重複するpolyfillを防ぎます。                                                                                         |

開発中にコードエディタ内で警告やエラーを表示するために、適切な[統合](https://eslint.org/docs/user-guide/integrations#editors)を使用することをおすすめします。

## 例 {#examples}

### カスタムディレクトリとファイルのリンティング {#linting-custom-directories-and-files}

デフォルトでは、Next.jsは`pages/`、`app/`、`components/`、`lib/`、および`src/`ディレクトリ内のすべてのファイルに対してESLintを実行します。しかし、`next.config.js`で生産ビルド用に`eslint`設定の`dirs`オプションを使用して、どのディレクトリを指定することができます：

```js title="next.config.js"
module.exports = {
  eslint: {
    dirs: ['pages', 'utils'], // 生産ビルド中に'pages'と'utils'ディレクトリでのみESLintを実行します (next build)
  },
}
```

同様に、特定のディレクトリやファイルをリンティングするために`next lint`で`--dir`と`--file`フラグを使用できます：

```bash title="ターミナル"
next lint --dir pages --dir utils --file bar.js
```

### モノレポ内のrootディレクトリの指定 {#specifying-a-root-directory-within-a-monorepo}

`eslint-plugin-next`をプロジェクト内で使用し、Next.jsがrootディレクトリにインストールされていない場合（モノレポなど）、`.eslintrc`の`settings`プロパティを使用して`eslint-plugin-next`にNext.jsアプリケーションの場所を知らせることができます：

```mjs title="eslint.config.mjs"
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
})

const eslintConfig = [
  ...compat.config({
    extends: ['next'],
    settings: {
      next: {
        rootDir: 'packages/my-app/',
      },
    },
  }),
]

export default eslintConfig
```

`rootDir`はパス（相対または絶対）、グロブ（例：`"packages/*/"`）、またはパスとグロブの配列です。

### キャッシュを無効にする {#disabling-the-cache}

パフォーマンスを向上させるために、ESLintで処理されたファイルの情報がデフォルトでキャッシュされます。これは`.next/cache`または既定の[ビルドディレクトリ](/docs/app/api-reference/config/next-config-js/distDir)に保存されます。単一のソースファイルの内容以上に依存するESLintルールを含む場合は、`next lint`で`--no-cache`フラグを使用してキャッシュを無効にします：

```bash title="ターミナル"
next lint --no-cache
```

### ルールを無効にする {#disabling-rules}

サポートされているプラグイン（`react`、`react-hooks`、`next`）から提供されるルールを変更または無効にしたい場合は、`.eslintrc`の`rules`プロパティを使用して直接変更できます：

```mjs title="eslint.config.mjs"
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
})

const eslintConfig = [
  ...compat.config({
    extends: ['next'],
    rules: {
      'react/no-unescaped-entities': 'off',
      '@next/next/no-page-custom-font': 'off',
    },
  }),
]

export default eslintConfig
```

### Core Web Vitals とともに {#with-core-web-vitals}

`next/core-web-vitals`ルールセットは、`next lint`が初めて実行され、**ストリクト**オプションが選択されたときに有効になります：

```mjs title="eslint.config.mjs"
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
})

const eslintConfig = [
  ...compat.config({
    extends: ['next/core-web-vitals'],
  }),
]

export default eslintConfig
```

`next/core-web-vitals`は、[Core Web Vitals](https://web.dev/vitals/)に影響を与えるルールにデフォルトでは警告となるいくつかのルールにエラーを設定します。

> 新しく[Create Next App](/docs/app/api-reference/cli/create-next-app)で作成されたアプリケーションに対して、`next/core-web-vitals`エントリポイントが自動的に含まれます。

### TypeScript とともに {#with-typescript}

Next.jsのESLintルールに加えて、`create-next-app --typescript`は`next/typescript`を使ってTypeScript固有のリンティングルールを設定に追加します：

```mjs title="eslint.config.mjs"
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
})

const eslintConfig = [
  ...compat.config({
    extends: ['next/core-web-vitals', 'next/typescript'],
  }),
]

export default eslintConfig
```

これらのルールは[`plugin:@typescript-eslint/recommended`](https://typescript-eslint.io/linting/configs#recommended)に基づいています。
詳細は[typescript-eslint > Configs](https://typescript-eslint.io/linting/configs)をご覧ください。

### Prettierとともに {#with-prettier}

ESLintにはコードフォーマットのルールも含まれており、既存の[Prettier](https://prettier.io/)の設定と競合する可能性があります。ESLintとPrettierを連携させるためには、[eslint-config-prettier](https://github.com/prettier/eslint-config-prettier)をESLint設定に含めることをおすすめします。

まず、依存関係をインストールします：

```bash title="ターミナル"
npm install --save-dev eslint-config-prettier

yarn add --dev eslint-config-prettier

pnpm add --save-dev eslint-config-prettier

bun add --dev eslint-config-prettier
```

次に、`prettier`を既存のESLint設定に追加します：

```mjs title="eslint.config.mjs"
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
})

const eslintConfig = [
  ...compat.config({
    extends: ['next', 'prettier'],
  }),
]

export default eslintConfig
```

### ステージされたファイルにリンティングを実行する {#running-lint-on-staged-files}

`next lint`を使用してステージされたgitファイルにリンターを実行するために[lint-staged](https://github.com/okonet/lint-staged)を使用したい場合は、プロジェクトのrootにある`.lintstagedrc.js`ファイルに以下を追加して、`--file`フラグの使用を指定する必要があります：

```js title=".lintstagedrc.js"
const path = require('path')

const buildEslintCommand = (filenames) =>
  `next lint --fix --file ${filenames
    .map((f) => path.relative(process.cwd(), f))
    .join(' --file ')}`

module.exports = {
  '*.{js,jsx,ts,tsx}': [buildEslintCommand],
}
```

## 生産ビルド中のリンティングを無効にする {#disabling-linting-during-production-builds}

`next build`中にESLintを実行しない場合は、`next.config.js`の`eslint.ignoreDuringBuilds`オプションを`true`に設定できます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  eslint: {
    // 警告: これにより、プロジェクトにESLintエラーがある場合でも本番ビルドが正常に完了できるようになります。
    ignoreDuringBuilds: true,
  },
}

export default nextConfig
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="next.config.js" switcher
const nextConfig = {
  eslint: {
    // 警告: これにより、プロジェクトにESLintエラーがある場合でも本番ビルドが正常に完了できるようになります。
    ignoreDuringBuilds: true,
  },
}

export default nextConfig
```

</TabItem>
</Tabs>

### 既存の設定を移行する {#migrating-existing-config}

すでにESLintがアプリケーションで設定されている場合、`eslint-config-next`を含めるのではなく、このプラグインから直接拡張することをおすすめします。ただし、以下の条件が満たされる場合を除きます。

#### 推奨プラグインルールセット {#recommended-plugin-ruleset}

以下の条件が真である場合：

- あなたは以下のプラグインの1つまたは複数をすでにインストールしています（別個に、または`airbnb`や`react-app`など別の設定を通じて）
  - `react`
  - `react-hooks`
  - `jsx-a11y`
  - `import`
- BabelがNext.js内で設定された方法とは異なる特定の`parserOptions`を定義しています（[Babelの設定をカスタマイズした](https://nextjs.org/docs/canary/pages/building-your-application/configuring/babel)場合を除いてこれは推奨されません）
- Node.jsおよび／またはTypeScriptの[リゾルバ](https://github.com/benmosher/eslint-plugin-import#resolvers)を使用してインポートを処理するために`eslint-plugin-import`をインストールしています

この場合、これらのプロパティが[`eslint-config-next`](https://github.com/vercel/next.js/blob/canary/packages/eslint-config-next/index.js)内でどのように設定されているかを好む場合は、それらの設定を削除するか、次のようにNext.js ESLintプラグインから直接拡張することをおすすめします：

```js
module.exports = {
  extends: [
    //...
    'plugin:@next/next/recommended',
  ],
}
```

プラグインは通常、プロジェクトにインストールされているため、`next lint`を実行する必要はありません：

```bash title="ターミナル"
npm install --save-dev @next/eslint-plugin-next

yarn add --dev @next/eslint-plugin-next

pnpm add --save-dev @next/eslint-plugin-next

bun add --dev @next/eslint-plugin-next
```

これは、複数の設定にまたがって同じプラグインやパーサをインポートすることによって発生する可能性のある衝突やエラーのリスクを排除します。

#### 追加の構成 {#additional-configurations}

別のESLint設定をすでに使用していて`eslint-config-next`を含めたい場合は、他の設定の後に最後に拡張することを確認してください。例えば：

```mjs title="eslint.config.mjs"
import js from '@eslint/js'
import { FlatCompat } from '@eslint/eslintrc'

const compat = new FlatCompat({
  // import.meta.dirname は Node.js v20.11.0 以降で利用可能です
  baseDirectory: import.meta.dirname,
  recommendedConfig: js.configs.recommended,
})

const eslintConfig = [
  ...compat.config({
    extends: ['eslint:recommended', 'next'],
  }),
]

export default eslintConfig
```

`next`の設定はすでに`parser`、`plugins`、`settings`プロパティのデフォルト値を設定しています。これらのプロパティを再宣言する必要はありません。ただし、使用例のために異なる設定が必要な場合を除きます。

他の共有可能な設定を含める場合は、**これらのプロパティが上書きされないまたは変更されないことを確認する必要があります**。それ以外の場合は、`next`の設定と共通の動作を共有する設定を削除するか、上記のようにNext.js ESLintプラグインから直接拡張することをおすすめします。
