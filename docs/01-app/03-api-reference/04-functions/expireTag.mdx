---
title: 'expireTag'
description: 'expireTag 関数のAPIリファレンス。'
version: 'canary'
---

`expireTag`は、特定のキャッシュタグに対する[キャッシュされたデータ](/docs/app/building-your-application/caching)をオンデマンドでパージすることを可能にします。

> **Good to know**:
>
> - `expireTag`は、[Node.js と Edge runtimes](/docs/app/building-your-application/rendering/edge-and-nodejs-runtimes)の両方で利用できます。
> - `expireTag`は、パスが次に訪問されたときにのみキャッシュを無効化します。したがって、`expireTag`を動的な route segment に対して呼び出した場合、すぐに多くの無効化をトリガーすることはありません。無効化は、次にパスが訪問されたときにのみ発生します。

## リファレンス {#reference}

### パラメータ {#parameters}

```tsx
expireTag(...tags: string[]): void;
```

- `tags`: 再検証したいデータに関連付けられたキャッシュタグを表す文字列引数。それぞれ256文字以内である必要があります。この値は大文字と小文字を区別します。

`fetch`にタグを次のように追加できます：

```tsx
fetch(url, { next: { tags: [...] } });
```

### 戻り値 {#returns}

`expireTag`は値を返しません。

## 使用例 {#examples}

### Server Action {#server-action}

`expireTag`をServer Actionの中で呼び出すことができます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="app/actions.ts" switcher
'use server'

import { expireTag } from 'next/cache'

export default async function submit() {
  await addPost()
  expireTag('posts', 'blog')
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/actions.js" switcher
'use server'

import { expireTag } from 'next/cache'

export default async function submit() {
  await addPost()
  expireTag('posts', 'blog')
}
```

</TabItem>
</Tabs>

### Route Handler {#route-handler}

`expireTag`をRoute Handlerの中で呼び出すことができます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="app/api/revalidate/route.ts" switcher
import type { NextRequest } from 'next/server'
import { expireTag } from 'next/cache'

export async function GET(request: NextRequest) {
  const tag = request.nextUrl.searchParams.get('tag')
  expireTag(tag)
  return Response.json({ revalidated: true, now: Date.now() })
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/api/revalidate/route.js" switcher
import { expireTag } from 'next/cache'

export async function GET(request) {
  const tag = request.nextUrl.searchParams.get('tag')
  expireTag(tag)
  return Response.json({ revalidated: true, now: Date.now() })
}
```

</TabItem>
</Tabs>
