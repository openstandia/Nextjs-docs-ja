---
title: 'unstable_expireTag'
description: 'unstable_expireTag 関数の API リファレンス'
version: 'unstable'
---

`unstable_expireTag` を利用すると、特定のキャッシュタグに対してキャッシュされた[データを](/docs/app/building-your-application/caching)オンデマンドで削除できます。

> **Good to know**:
>
> - `unstable_expireTag` は、[Node.js と Edge runtime の両方](/docs/app/building-your-application/rendering/edge-and-nodejs-runtimes)で利用できます。
> - `unstable_expireTag` は、パスが次に訪問されたときにのみキャッシュを無効化します。つまり、dynamic route segment を使用して `unstable_expireTag` を呼び出しても、すぐに多くの無効化を一度に引き起こすことはありません。無効化は、パスが次に訪れたときにのみ発生します。

## リファレンス {#reference}

### パラメータ {#parameters}

```tsx
unstable_expireTag(...tags: string[]): void;
```

- `tags`: 再検証したいデータに関連付けられたキャッシュタグを表す文字列引数です。それぞれ256文字以下である必要があります。この値は大文字と小文字を区別します。

次のように `fetch` にタグを追加できます：

```tsx
fetch(url, { next: { tags: [...] } });
```

### 戻り値 {#returns}

`unstable_expireTag` は、値を返しません。

## 例 {#examples}

### Server Action {#server-action}

Server Action 内で `unstable_expireTag` を呼び出すことができます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="app/actions.ts" switcher
'use server'

import { unstable_expireTag } from 'next/cache'

export default async function submit() {
  await addPost()
  unstable_expireTag('posts', 'blog')
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/actions.js" switcher
'use server'

import { unstable_expireTag } from 'next/cache'

export default async function submit() {
  await addPost()
  unstable_expireTag('posts', 'blog')
}
```

</TabItem>
</Tabs>

### Route Handler {#route-handler}

Route Handler 内で `unstable_expireTag` を呼び出すことができます：

<Tabs>
<TabItem value="ts" label="TypeScript">

```ts title="app/api/revalidate/route.ts" switcher
import type { NextRequest } from 'next/server'
import { unstable_expireTag } from 'next/cache'

export async function GET(request: NextRequest) {
  const tag = request.nextUrl.searchParams.get('tag')
  unstable_expireTag(tag)
  return Response.json({ revalidated: true, now: Date.now() })
}
```

</TabItem>
<TabItem value="js" label="JavaScript">

```js title="app/api/revalidate/route.js" switcher
import { unstable_expireTag } from 'next/cache'

export async function GET(request) {
  const tag = request.nextUrl.searchParams.get('tag')
  unstable_expireTag(tag)
  return Response.json({ revalidated: true, now: Date.now() })
}
```

</TabItem>
</Tabs>
